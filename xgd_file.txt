LDPC：
100（不启动）
101（启动）
100（不启动）
Turbo:
000（不启动）
001（启动）
000（不启动）
高速扩频：
280（不启动）
281（启动）
280（不启动）
低速扩频：
200（不启动）
201（启动）
200（不启动）


有用信号+梳状谱干扰，ISR=1dB：//门限6000
C400（不启动）
C401（启动）
C400（不启动）

有用信号+高斯白噪声，ISR=10dB：//门限6000
8800（不启动）
8801（启动）
8800（不启动）
